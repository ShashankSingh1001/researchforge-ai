# web framework
fastapi==0.111.0
uvicorn==0.30.1
python-multipart==0.0.27

# pydantic (fastapi dependency, pin to avoid breaking changes)
pydantic==2.7.4

# LLM client
groq==0.9.0

# agent framework
langgraph==0.1.19
langchain-core==0.2.22

# search + scraping + PDF
tavily-python==0.7.24
requests==2.32.3
beautifulsoup4==4.12.3
lxml==6.1.1
pymupdf==1.24.5

# RAG + embeddings + vector DB
sentence-transformers==3.0.1
faiss-cpu==1.8.0
numpy==1.26.4

# torch CPU-only — replaces full torch to stay within Render free tier memory limits
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu

# utilities
python-dotenv==1.0.1
tenacity==8.5.0

# frontend
streamlit==1.36.0
reportlab==4.2.2

# tests
pytest==9.0.3